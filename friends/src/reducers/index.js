const initialState = {
  error: "",
  fetchingData: false,
  gasPrices: []
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
